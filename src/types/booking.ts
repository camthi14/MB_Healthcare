export type BookingInfoCustomerPayload = {
  phone_number: string;
  first_name: string;
  last_name: string;
  birth_date: string;
  email: string;
};

export type BookingForPayload = {
  reason: string;
};
